{
  "name": "mihenk",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --env-file=.env hadis-api.mjs",
    "build-data": "node ingest.mjs && node ingest-kuran.mjs",
    "build-vectors": "node embed-kuran.mjs && node embed-hadis.mjs",
    "build": "npm run build-data && npm run build-vectors"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@huggingface/transformers": "^4.2.0",
    "adhan": "^4.4.4",
    "tz-lookup": "^6.1.25"
  }
}
